# Installation

```bash
pip install langchain langchain-text-splitters langchain-community pypdf openpyxl python-magic-bin
```

Or install from requirements file:

```bash
cd doc_chunker
pip install -r requirements.txt
```
